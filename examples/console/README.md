# Console example with textual

This is an example on how to use uulpy in a terminal with [textual](https://github.com/Textualize/textual).

## How to run

- Install the `textual` package with `pip install textual`
- Run `main.py`